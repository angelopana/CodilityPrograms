package main

func main() {

	triangle()
	// reader := bufio.NewScanner(os.Stdin)
	// var numbers int
	// var sentences string

	// for reader.Scan() {
	// fmt.Println(reflect.TypeOf(reader.Text()))

	// Testing functions, below converts the string into int
	// holder, err := strconv.Atoi(reader.Text())
	// if err != nil {
	// 	fmt.Println("Error")
	// }

	// fmt.Println(Solution(holder))
	// fmt.Println(fibRecur(holder))
	// fmt.Println(fibRecur1(holder))
	// fmt.Println(fibIter(holder))
	// fmt.Println(uniqueChar(reader.Text()))
	// reverse(reader.Text())

	// }

}
